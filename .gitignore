# Maven build output
target/

# Local lib jars. Fabled is a system-scoped compile dep (see pom.xml), so it
# MUST be tracked for CI to build; ignore everything else in lib/.
lib/*
!lib/fabled.jar

# IDE
.idea/
*.iml
.vscode/
